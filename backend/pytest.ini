[pytest]
testpaths = tests
asyncio_mode = strict
markers =
    integration: gated Postgres-backed integration tests (opt-in via RUN_INTEGRATION_TESTS=1; needs Docker). Skipped by default so the standard suite stays fast and offline.
