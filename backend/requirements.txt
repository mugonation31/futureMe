# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0

# JWT token verification
PyJWT==2.8.0
python-multipart==0.0.6

# Password hashing
passlib[bcrypt]==1.7.4

# Database (async PostgreSQL)
asyncpg==0.29.0

# Environment variables
python-dotenv==1.0.0

# HTTP client
httpx==0.26.0

# Pydantic for data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Email (password reset)
resend==2.30.1

# Testing (dev dependencies)
pytest==7.4.4
pytest-asyncio==0.23.3

# Integration testing (opt-in, gated by RUN_INTEGRATION_TESTS=1).
# Spins up an ephemeral Postgres via Docker to prove SQL-level tenant isolation.
# Not needed for the default offline test run.
testcontainers[postgres]==4.14.2
